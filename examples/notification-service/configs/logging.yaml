# Schema: api/config/infra/logging/config.proto
# Format: YAML (enum values may be numeric or string in prototext)
kind: logging
api_version: v1
spec:
  level: 2  # INFO (see proto Spec.Level)
  encoding: 1  # JSON
